version: "3"

services:
  app:
    build: .
    # restart: always
    depends_on:
      - mongo
    ports:
      - "3000:3000"

  mongo:
    image: mongo
    # restart: always
    ports:
      - "27017:27017"
    volumes:
      - mongodb-data:/data/db
volumes:
  mongodb-data:
    driver: local
